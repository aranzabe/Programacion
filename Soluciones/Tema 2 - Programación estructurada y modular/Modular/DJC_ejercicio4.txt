Lógico esPositivo(num es real)
    positivo es lógico
Inicio
    Si (num >= 0)
        positivo = Verdadero
    sino
        positivo = Falso
    FinSi
    
    Devolver (positivo)
Fin
------------------------------------------------------------
Algoritmo principal
    n es real
    posit es lógico

Inicio
    Escribir "Este programa te dice si un número es positivo o negativo"
    Leer n
    
    Si (esPositivo(n) == Verdadero)
        raiz = rc(n)
        Escribir raiz
    SiNo
        Escribir "No se puede hacer la raíz porque es negativo."
    FinSi
Fin 
